# Architecture Knowledge

Store durable fork-specific design knowledge here: subsystem boundaries,
integration contracts, platform constraints, and decisions that future changes
must respect.

Create one concise record per decision or subsystem. Update the record when the
decision evolves, and mark it `Superseded` with a successor link when it no
longer applies.